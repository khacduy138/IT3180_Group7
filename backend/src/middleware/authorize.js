/*
 * Authorize middleware placeholder.
 *
 * Purpose:
 * - Check role or permission after authenticate has already run.
 * - Return 403 when logged-in user does not have enough permission.
 *
 * TODO:
 * - Accept required role or permission.
 * - Check req.user.role or req.user.permissions.
 * - Call next() when allowed.
 * - Return 403 when denied.
 */
